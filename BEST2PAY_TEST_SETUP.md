# Best2Pay Test Environment Setup

Инструкция по настройке тестового окружения для Best2Pay интеграции.

## Компоненты

1. **Тестовый бот** - с тестовыми настройками Best2Pay
2. **Тестовая панель Remnawave** - на этом же хосте (/opt/remnawave)
3. **Nginx** - для обработки вебхуков с SSL
4. **Test API Best2Pay** - test.best2pay.net

## Шаг 1: Настройка Nginx и SSL

### 1.1. Установить certbot (если еще нет)

```bash
sudo apt update
sudo apt install certbot python3-certbot-nginx -y
```

### 1.2. Скопировать конфиг nginx

```bash
sudo cp nginx-webhooks.conf /etc/nginx/sites-available/wh.onvpn.online
sudo ln -s /etc/nginx/sites-available/wh.onvpn.online /etc/nginx/sites-enabled/
```

### 1.3. Получить SSL сертификат

```bash
# Сначала временно отключаем SSL в конфиге для получения сертификата
sudo certbot certonly --nginx -d wh.onvpn.online

# После получения сертификата активируем полный конфиг
sudo nginx -t
sudo systemctl reload nginx
```

### 1.4. Проверить работу

```bash
curl https://wh.onvpn.online/health
# Должен вернуть: healthy
```

## Шаг 2: Настройка .env для тестов

Создайте `.env.test` или измените существующий `.env`:

```bash
# Best2Pay Test Configuration
BEST2PAY_SECTOR_ID=8365
BEST2PAY_PASSWORD=I3ssq1q:UiM2ozdvb
BEST2PAY_API_URL=https://test.best2pay.net/webapi/
BEST2PAY_ENABLED=True

# Webhook URL
WEBHOOK_BASE_URL=https://wh.onvpn.online

# Test Panel API (получить от админа)
PANEL_API_URL=http://localhost:8000/api
PANEL_API_KEY=your_test_panel_api_key_here

# Отключить другие провайдеры для чистоты тестов
YOOKASSA_ENABLED=False
STARS_ENABLED=False
TRIBUTE_ENABLED=False
CRYPTOPAY_ENABLED=False
FREEKASSA_ENABLED=False
NOWPAYMENTS_ENABLED=False
```

## Шаг 3: Запуск тестового окружения

### 3.1. Остановить продакшн бот (если нужно)

```bash
cd /root/remna-tg-bot
docker compose down
```

### 3.2. Запустить с тестовым конфигом

```bash
# Применить миграции (если есть изменения в БД)
docker compose run --rm remnawave-tg-shop python db/migrator.py

# Запустить бота
docker compose up -d
```

### 3.3. Проверить логи

```bash
docker compose logs -f remnawave-tg-shop
```

Должны увидеть в логах:
```
Best2Pay service configured:
  Sector ID: 8365
  Sector UUID: None
  API URL: https://test.best2pay.net/webapi/
  Using for API: 8365
```

## Шаг 4: Использование тестового меню

1. Откройте Telegram бот
2. Отправьте команду `/admin`
3. Выберите "⚙️ Системные функции"
4. Нажмите "🧪 Тестирование Best2Pay"

### Пошаговое тестирование:

#### 4.1. Создать тестового пользователя
- Нажмите "1️⃣ Создать тестового пользователя"
- Скопируйте UUID для следующих шагов
- Пользователь будет создан в тестовой панели с префиксом `test_user_`

#### 4.2. Создать тестовый платеж
- Нажмите "2️⃣ Создать тестовый платеж"
- Выберите период подписки (например, 1 месяц - 300₽)
- Платеж будет зарегистрирован в Best2Pay
- Запомните Order ID

#### 4.3. Сформировать ссылку на оплату
- Нажмите "3️⃣ Сформировать ссылку на оплату"
- Будет создана СБП ссылка (может не работать на test стенде)

#### 4.4. Симулировать успешную оплату
- Нажмите "4️⃣ Симулировать успешную оплату"
- Best2Pay отправит webhook на бот
- Проверьте логи бота - должен прийти webhook
- Подписка должна активироваться автоматически

#### 4.5. Проверить статус подписки
- Нажмите "6️⃣ Проверить статус подписки"
- Увидите детальную информацию о пользователе, подписке и платежах

#### 4.6. Очистить тестовые данные
- Нажмите "🗑️ Очистить тестовые данные"
- Подтвердите удаление
- Пользователь будет удален из панели, FSM очищен

## Шаг 5: Проверка вебхуков

### Логи nginx

```bash
tail -f /var/log/nginx/wh.onvpn.online_access.log
tail -f /var/log/nginx/wh.onvpn.online_error.log
```

### Логи бота

```bash
docker compose logs -f remnawave-tg-shop | grep TEST_B2P
```

Должны увидеть:
```
[TEST_B2P] User created: username=test_user_1234567890, uuid=...
[TEST_B2P] Payment created: payment_id=123, order_id=2804372329
[TEST_B2P] Payment simulated: order_id=2804372329, success=True
```

### Проверка webhook напрямую

```bash
curl -X POST https://wh.onvpn.online/202321/best2pay/notify \
  -H "Content-Type: application/xml" \
  -d '<operation><order_id>test</order_id></operation>'
```

## Шаг 6: Тестирование неуспешной оплаты

1. Создайте нового пользователя и платеж (шаги 1-3)
2. Нажмите "5️⃣ Симулировать неуспешную оплату"
3. Проверьте логи - должен прийти webhook со статусом DECLINED
4. Подписка НЕ должна активироваться

## Отладка

### Проблема: Webhook не приходит

1. Проверьте nginx:
```bash
sudo nginx -t
sudo systemctl status nginx
```

2. Проверьте, что бот слушает на порту 8080:
```bash
netstat -tlnp | grep 8080
```

3. Проверьте firewall:
```bash
sudo ufw status
sudo ufw allow 443/tcp
```

### Проблема: Ошибка создания пользователя в панели

1. Проверьте PANEL_API_URL в .env
2. Проверьте PANEL_API_KEY
3. Проверьте логи панели:
```bash
cd /opt/remnawave
# смотрите логи панели
```

### Проблема: Best2Pay возвращает ошибку

1. Проверьте BEST2PAY_SECTOR_ID
2. Проверьте BEST2PAY_PASSWORD
3. Проверьте что используется test.best2pay.net:
```bash
docker compose exec remnawave-tg-shop env | grep BEST2PAY
```

## Возврат к продакшн

```bash
# Восстановите оригинальный .env
cp .env.prod .env  # если сохраняли

# Перезапустите бота
docker compose down
docker compose up -d
```

## Полезные команды

```bash
# Просмотр всех тестовых пользователей в логах
docker compose logs remnawave-tg-shop | grep "test_user_"

# Очистка всех контейнеров и перезапуск
docker compose down && docker compose up -d --build

# Проверка SSL сертификата
openssl s_client -connect wh.onvpn.online:443 -servername wh.onvpn.online

# Проверка DNS
nslookup wh.onvpn.online
```

## Контрольный чеклист

- [ ] Nginx настроен и работает
- [ ] SSL сертификат получен и активен
- [ ] .env содержит тестовые креды Best2Pay
- [ ] Webhook URL правильный (wh.onvpn.online)
- [ ] Панель API доступна и работает
- [ ] Бот запущен и подключен к БД
- [ ] Тестовое меню доступно в /admin
- [ ] Успешно создан тестовый пользователь
- [ ] Успешно создан и зарегистрирован платеж
- [ ] Webhook приходит и обрабатывается
- [ ] Подписка активируется после успешной симуляции
- [ ] Тестовые данные очищаются корректно

---

**Версия:** 1.0
**Дата:** 2025-11-18
**Поддержка:** Проверяйте логи бота и nginx
